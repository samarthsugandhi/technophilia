import EventFlowClient from "./EventFlowClient";

export const metadata = {
  title: "Event Flow — TECHNOPHILIA 3.0",
  description: "Explore the two-day event structure, qualifying rounds, and general instructions for TECHNOPHILIA 3.0.",
};

export default function EventFlowPage() {
  return <EventFlowClient />;
}
